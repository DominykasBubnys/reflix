import { StyleSheet } from "react-native";

const Styles = StyleSheet.create({
    loadingSpinner: {
        marginTop: "20%",
        padding: 10,

    },

    text: {
        color: "white",
        textAlign: "center",
        fontSize: 35,
        fontWeight: "bold",
    }
})

export default Styles;